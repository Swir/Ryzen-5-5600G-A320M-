name: README Docs

on:
  pull_request:
    paths:
      - "README.md"
      - "STATUS.md"
      - "assets/readme/**"
      - "scripts/update_readme_progress.py"
      - ".github/workflows/readme-docs.yml"
  push:
    branches: [main]
    paths:
      - "README.md"
      - "STATUS.md"
      - "assets/readme/**"
      - "scripts/update_readme_progress.py"
      - ".github/workflows/readme-docs.yml"

permissions:
  contents: read

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Verify generated SVG and legacy-meter cleanup
        run: python scripts/update_readme_progress.py --check
